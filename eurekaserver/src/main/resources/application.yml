server: #当前 Eureka Server  服务端口
  port: 8761
eureka:
  client:
#    是否将当前到 Eureka Server 服务作为客户端进行注册
    register-with-eureka: false
    #是否获取其他 Eureka Server 服务的数据
    fetch-registry: false
    #注册中心的访问地址
    service-url:
      defaultZone:  http://localhost:8761/eureka/
